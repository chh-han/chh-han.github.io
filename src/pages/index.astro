---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Home from '@/components/pages/Home';
import { buildPersonJsonLd } from '@/data/site';

const personJsonLd = buildPersonJsonLd(Astro.site?.toString());
---

<BaseLayout title="ChangHeon Han" jsonLd={personJsonLd}>
  <Home />
</BaseLayout>
